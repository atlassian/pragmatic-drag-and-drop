export const gridSize = 8;

/**
 * The space between each column in a board.
 */
export const columnGap = 2 * gridSize;

/**
 * The space between each card in a column.
 */
export const cardGap = gridSize;
